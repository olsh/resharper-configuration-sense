namespace Resharper.ConfigurationSense.Tests.Constants
{
    // The names the plugin matches on, passed to DoNamedTest2 to put the file in the test project
    internal static class ConfigurationFiles
    {
        public const string AppConfig = "app.config";

        public const string AppSettingsJson = "appsettings.json";

        public const string WebConfig = "web.config";
    }
}
